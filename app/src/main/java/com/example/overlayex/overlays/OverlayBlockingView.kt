package com.example.overlayex.overlays

import android.content.Context
import android.view.View

class OverlayBlockingView(context: Context) : View(context) {

    init {
        // Add any necessary customizations to the view
    }

    // Override necessary methods like onMeasure, onDraw, etc. if required
}
